def setup(client) -> None:
    ...
    #! i will reimplement this later, probably in the form of a separate project
    #! and then integrates with the bot through an api
    #! not worth the extra weight on the bot process
